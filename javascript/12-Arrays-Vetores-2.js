
//num.sort() // organiza em ordem crescente
//num.push() // adiciona mais um espaço no vetor
//num.length // mostra quantos espaços tem no vetor
//num[chave] // mostra o valor que tem na chave
//num.indexOf(valor) // mostra a chave desse valor



// Maneira de usar o For de forma padrão:

/* for (let after = 0; after < number.length; after++) {
  console.log(`A posição ${after}, tem valor dê ${number[after]}`);
}  */


// Maneira simplificada de usar o for nos vetores:

/* for (let after in number) {
  console.log(number[after]);
} */
